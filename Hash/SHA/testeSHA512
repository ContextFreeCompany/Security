#Teste da classe SHA512

require_relative 'SHA512'

sha = SHA512.new "Giovani Ferreira"

# puts sha.hash

# puts sha.is_password? "Giovani Ferreira"

sha2 = SHA512.new sha.prepareForSave[:secret], sha.prepareForSave[:salt]

puts sha2 == "Giovani Ferreira"